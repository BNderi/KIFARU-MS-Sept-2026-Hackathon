# Optional. Without these the agent uses its deterministic explanation fallback,
# so the demo never depends on a live network call.
AZURE_OPENAI_ENDPOINT=https://<resource>.openai.azure.com
AZURE_OPENAI_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
KIFARU_DB=./data/kifaru.db
